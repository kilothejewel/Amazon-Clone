import React from 'react';
import './hero.css';

function Hero() {
  return (
    <section className="hero">
      {/* Image is handled via CSS background */}
    </section>
  );
}

export default Hero;
